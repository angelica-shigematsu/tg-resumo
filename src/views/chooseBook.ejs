<%- include('partials/nav.ejs')  %> 
<%- include('partials/header.ejs') %> 

<body>
  <div class="container-sm">
    <form class="mb-3" id="form-summary-submit" enctype="application/x-www-form-urlencoded"
    method="POST" action="/titulo">
      <h3>Cadastro de Resumo</h3>
      <div class="mb-3 row">
        <label for="" class="col-sm-2 col-form-label">Título</label>
        <div class="col-sm-10">
          <select class="form-select col-sm-6" aria-label="Default select example" name="id" required>
            <%books.forEach((book) => {%>
              <option value="<%= book.id %>"><%= book.title %></option>
              <%})%>
          </select>
        </div>
      </div>
      
      <div class="col-sm-3">
        <button class="btn btn-outline-success col" form="form-summary-submit" type="submit">Pesquisar</button>
      </div>
    </form>
  </div>
  
<%- include('partials/footer.ejs') %> 
    